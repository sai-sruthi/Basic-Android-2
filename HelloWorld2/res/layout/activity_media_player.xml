<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MediaPlayerActivity" >

    <Button
        android:id="@+id/btnmppause"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="23dp"
        android:layout_marginTop="140dp"
        android:layout_toRightOf="@+id/btnmpstop"
        android:text="Pause" />

    <Button
        android:id="@+id/btnmpstop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/btnmppause"
        android:layout_alignBottom="@+id/btnmppause"
        android:layout_marginLeft="38dp"
        android:layout_toRightOf="@+id/btnmpplay"
        android:text="Stop" />

    <Button
        android:id="@+id/btnmpplay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignTop="@+id/btnmppause"
        android:text="Play" />

    <TextView
        android:id="@+id/txtmailto"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btnmpstop"
        android:layout_alignParentTop="true"
        android:layout_marginTop="43dp"
        android:text="Audio Player" />

</RelativeLayout>